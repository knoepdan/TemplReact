import * as React from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface Props {}

// more or less random notes about webpack setup
export const StatisticsPage = (): React.ReactElement<Props> => {
    return <div>Statistics page</div>;
};

export default StatisticsPage as React.FC;
